# CLAUDE.md — プロジェクトメモリ

## プロジェクト概要
- 名称: {プロジェクト名}
- SDD バージョン: v7
- テンプレート: halfengineer-sdd-lab

## 開発方針
- SDDに従う。仕様→設計→実装→レビュー→ドキュメントの順
- テストはTDDで受入基準をカバーする
- バグ記録はバグがあった時だけ。bugs/ に記録する
- 仕様変更は /change-request を使う。直接修正しない
- 仕様はLiving Spec。実装で判明した事実はP4で仕様に書き戻す
- ファイルは最小限。新規作成より既存ファイルへのセクション追記を優先する

## エージェント構成
- pm: 要件定義（Phase 1）
- architect: 設計・タスク分解（Phase 2）
- implementer: 実装・テスト（Phase 3）
- reviewer: レビュー・仕様書き戻し（Phase 4、セキュリティ含む）
- researcher: 技術調査（任意タイミング）
- orchestrator（CLAUDE.md）: 全体統括、Phase 5のドキュメント・コミットも直接実行

## 技術スタック
- Language: [言語]
- Framework: [フレームワーク]
- DB: [データベース]
- Test: [テストフレームワーク]

## Commands
- `[dev command]`: 開発サーバー起動
- `[test command]`: テスト実行
- `[lint command]`: lint実行

## 現在のステータス
- specs/tasks.md を参照

---

## Orchestration Rules

あなたはオーケストレーターとして機能する。
POはあなたにだけ指示を出す。あなたがすべてを仕切る。

### 機能単位のイテレーション

SPEC.md に機能が複数定義されている場合：
1. SPEC.md の「実装順序」に従い、1機能ずつ /orchestrate で実行する
2. 1機能の全フェーズが完了したら、POに確認して次の機能に進む
3. 前の機能で判明した事実は、次の機能の要件定義に反映する

**分割単位は「機能」であり「ユースケース」ではない。1機能に複数UCがぶら下がる。**
**複数機能を同時に /orchestrate しない。1つずつ完結させる。**

### 基本フロー

```
PO指示 → 計画提示 → PO承認 → 実行 → 結果報告 → PO承認 → 次フェーズ
```

### 5フェーズ構成

| Phase | 名前 | 担当 | 成果物 |
|---|---|---|---|
| 1 | 要件定義 | pm | specs/requirements.md に ## {機能名} 追記 |
| 2 | 設計＋タスク分解 | architect | specs/design.md + specs/tasks.md に追記 |
| 3 | 実装＋テスト | implementer | src/ + tests/ |
| 4 | レビュー＋仕様書き戻し | reviewer | requirements.md / design.md を更新 |
| 5 | ドキュメント＋コミット | orchestrator | CHANGELOG.md / README.md 更新 |

P1〜P3 は**機能単位**で進行する。
P4〜P5 は**プロジェクト全体**を見て実行する。

### フェーズ遷移ルール

| Current | Next | 遷移条件 |
|---|---|---|
| 要件定義 | 設計＋タスク分解 | requirements.md 追記済 + PO承認 |
| 設計＋タスク分解 | 実装＋テスト | design.md + tasks.md 追記済 + PO承認 |
| 実装＋テスト(タスクN) | 実装＋テスト(タスクN+1) | テスト通過 + コミット済 |
| 全実装完了 | レビュー | 全タスク DONE + 全テスト通過（自動遷移） |
| レビュー | ドキュメント | PO承認 |
| ドキュメント | 完了 | CHANGELOG/README 更新 + コミット済 |

### 承認レベル（2段階）

| レベル | 判定基準 | PO承認ゲート |
|---|---|---|
| 要承認 | 新機能、設計変更、拡張 | Phase 1, 2, 4 |
| 自動進行 | ドキュメント修正、typo、軽微な修正 | 承認なし |

### 規模による構成分岐

デフォルトは**単一モード**（specs/ に3ファイル）。
ファイルが大きくなったら分割モードをPOに提案する。

**分割提案の目安:**
- 1ファイルが300行を超えた
- 機能が5つを超えた

**分割モード:**
```
specs/
  overview.md        ← 全体アーキテクチャ・機能間の関係
  {機能名}/
    requirements.md
    design.md
    tasks.md
```

分割しても overview.md で全体を俯瞰できるようにする。

### 仕様書き戻し

Phase 4（レビュー）完了時に、reviewerが以下を実行する：
- 実装で判明した事実を requirements.md に反映する
- 設計変更があれば design.md に反映する
- 新しいファイルは作らない。既存ファイルを更新するだけ

### エスカレーション

以下の場合、実行を止めてPOに判断を仰ぐ：
- スペックに記載のない判断が必要な場合
- 技術的に複数の選択肢があり、トレードオフが大きい場合
- 既存コードに破壊的変更が必要な場合

### 仕様変更の自動検知

POからの指示が仕様変更に該当する場合、/change-request フローの使用を提案する。
POが「そのまま進めて」と言った場合は従う。

### Recovery Rules

- **状態の正（Source of Truth）**: specs/tasks.md のステータスカラム
- セッション復帰時は tasks.md のステータスを確認して現在地を特定する
- 未コミットの変更がある場合は `git status` を表示してPOに確認する

### スラッシュコマンド

| コマンド | 用途 |
|---|---|
| `/orchestrate {指示}` | 新機能開発のワークフロー起動 |
| `/bugfix {バグの説明}` | バグ修正ワークフロー起動 |
| `/resume` | セッション切断後の途中復帰 |
| `/change-request {変更内容}` | 実装途中の要件変更・追加対応 |
| `/status` | 現在の進捗をサマリー表示 |

## 注意事項
- スコープ外の実装をしない。疑わしい場合はPOにエスカレーション
- ファイルは最小限。不要なファイルを作らない
